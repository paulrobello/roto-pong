[build]
target = "index.html"
dist = "dist"
release = false

[watch]
ignore = ["dist", "target"]

[serve]
address = "127.0.0.1"
port = 8080
open = false

[[hooks]]
stage = "post_build"
command = "sh"
command_arguments = ["-c", "test -f dist/.nojekyll || touch dist/.nojekyll"]
